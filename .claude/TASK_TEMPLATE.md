# Task Templates for C3PO Project

Используйте эти шаблоны для создания технических заданий (ТЗ) при разработке новых компонентов, features, widgets и других элементов проекта.

---

## 📋 Минимальный шаблон (для быстрых задач)

Копируйте и заполняйте для простых компонентов:

```markdown
## [Название компонента]

**Слой:** [shared/entities/features/widgets/pages]
**Путь:** [точный путь, например: features/task-filter]

**Что делает:**
[Одна строка описания]

**Функции:**
- [ ]
- [ ]
- [ ]

**UI:**
[Компоненты shadcn или описание]

**Интеграция:**
[Куда добавить/использовать]
```

### Пример использования минимального шаблона

```markdown
## Theme Switcher

**Слой:** features
**Путь:** features/theme-switcher

**Что делает:**
Кнопка переключения темы приложения (light/dark)

**Функции:**
- [ ] Показывает текущую тему иконкой (Sun/Moon)
- [ ] При клике переключает тему
- [ ] Использует mode-watcher

**UI:**
Button (icon variant), иконки Sun/Moon из lucide

**Интеграция:**
Добавить в NavBar widget справа
```

---

## 📝 Полный шаблон (для сложных компонентов)

Копируйте и заполняйте для сложных features/widgets:

```markdown
## [Название компонента]

### Основное
- **Слой:** [shared/entities/features/widgets/pages]
- **Путь:** [features/task-filter]
- **Тип:** [компонент/утилита/тип/страница]

### Описание
[Что делает компонент, зачем нужен, контекст использования]

### Функциональность

**Основные функции:**
- [ ] [Функция 1]
- [ ] [Функция 2]
- [ ] [Функция 3]

**Состояния:**
- [ ] Loading (загрузка данных)
- [ ] Error (обработка ошибок)
- [ ] Empty state (пустое состояние)
- [ ] Success (успешное состояние)

**Взаимодействия:**
- [Описание пользовательских взаимодействий: клики, ввод, drag-n-drop и т.д.]

### UI/UX

**Компоненты shadcn:**
- [Button, Card, Input, и т.д.]
- [Указать конкретные компоненты из @/shared/ui]

**Иконки:**
- [Названия иконок из lucide-svelte]

**Стили:**
- [Цветовая схема, размеры, отступы]
- [Анимации и переходы]

**Адаптивность:**
- Desktop: [поведение на десктопе]
- Mobile: [поведение на мобильных]

### Данные

**Props:**
```typescript
interface ComponentProps {
	// Определите интерфейс props
	// Например:
	// items: Task[];
	// onSelect?: (item: Task) => void;
}
```

**Events/Bindings:**
- [События, которые компонент должен генерировать]
- [Двусторонние привязки через $bindable]

**Источники данных:**
- [API endpoints, stores, локальное состояние]

**Зависимости:**
- [Другие features/entities, которые использует]

### Интеграция

**Где использовать:**
- [Конкретные страницы или компоненты]

**Роутинг (если нужен):**
- [Новые роуты в src/routes/]
- [Обновления в entities/nav-bar/routes.ts]

### Дополнительно
- [ ] Тесты нужны?
- [ ] Accessibility требования (aria-labels, keyboard navigation)
- [ ] Анимации (укажите библиотеку или Tailwind)
- [ ] Локализация (если нужна)
- [ ] Другие требования
```

### Пример использования полного шаблона

```markdown
## Task List Widget

### Основное
- **Слой:** widgets
- **Путь:** widgets/task-list
- **Тип:** компонент

### Описание
Виджет для отображения списка задач с возможностью фильтрации,
отметки выполнения и удаления. Используется на странице планировщика
для управления ежедневными задачами.

### Функциональность

**Основные функции:**
- [ ] Отображение списка задач из массива
- [ ] Checkbox для отметки задачи как выполненной
- [ ] Кнопка удаления задачи
- [ ] Фильтры: все/активные/завершенные
- [ ] Счетчик активных задач

**Состояния:**
- [ ] Loading (при загрузке с API)
- [ ] Error (если не удалось загрузить)
- [ ] Empty state ("Нет задач", мотивационное сообщение)
- [ ] Success (список задач)

**Взаимодействия:**
- Клик на checkbox - переключает статус задачи
- Клик на кнопку удаления - показывает подтверждение, затем удаляет
- Клик на фильтр - переключает отображаемые задачи

### UI/UX

**Компоненты shadcn:**
- Card, CardHeader, CardTitle, CardContent
- Checkbox из @/shared/ui/checkbox
- Button (variant: ghost для удаления)
- Tabs для фильтров

**Иконки:**
- Trash2 (для удаления)
- Check (для выполненных задач)
- ListTodo (для заголовка)

**Стили:**
- Выполненные задачи: line-through, text-muted-foreground
- Hover эффект на строках задач
- Плавная анимация появления/удаления (animate-in/out)

**Адаптивность:**
- Desktop: фиксированная ширина, справа от календаря
- Mobile: full width, под календарем

### Данные

**Props:**
```typescript
interface Task {
	id: string;
	title: string;
	completed: boolean;
	createdAt: Date;
}

interface TaskListProps {
	initialTasks?: Task[];
	onTaskUpdate?: (task: Task) => void;
	onTaskDelete?: (id: string) => void;
}
```

**Events/Bindings:**
- `onTaskUpdate` - срабатывает при изменении статуса задачи
- `onTaskDelete` - срабатывает при удалении задачи

**Источники данных:**
- Начальные данные через props
- Локальное состояние через $state для фильтров

**Зависимости:**
- Нет зависимостей от других features

### Интеграция

**Где использовать:**
- Страница /planner справа от календаря features/calendar
- Использовать grid layout для размещения

**Роутинг (если нужен):**
- Роутинг не требуется, используется внутри существующей страницы

### Дополнительно
- [ ] Тесты не требуются на первом этапе
- [ ] Accessibility: aria-label для кнопок, keyboard navigation (Enter, Delete)
- [ ] Анимации: Tailwind animate-in, animate-out
- [ ] Добавить drag-n-drop для сортировки (опционально, в будущем)
```

---

## 🎯 Рекомендации по использованию

### Когда использовать минимальный шаблон:
- ✅ Простые UI компоненты
- ✅ Небольшие features с очевидной логикой
- ✅ Быстрые итерации и прототипирование
- ✅ Изменения в существующих компонентах

### Когда использовать полный шаблон:
- ✅ Новые widgets с композицией из нескольких features
- ✅ Сложная бизнес-логика
- ✅ Интеграция с API
- ✅ Компоненты с множеством состояний
- ✅ Когда важна accessibility и адаптивность

### Обязательные поля в любом шаблоне:
1. **Слой FSD** - критически важно для правильной архитектуры
2. **Путь** - точное расположение в структуре проекта
3. **Что делает** - краткое описание назначения
4. **Интеграция** - где и как будет использоваться

---

## 💡 Полезные ссылки

- [CLAUDE.md](../CLAUDE.md) - Руководство по проекту для Claude Code
- [FSD Documentation](https://feature-sliced.design/) - Feature-Sliced Design
- [Svelte 5 Docs](https://svelte-5-preview.vercel.app/) - Svelte 5 runes
- [shadcn-svelte](https://www.shadcn-svelte.com/) - UI компоненты

---

**Совет:** Копируйте нужный шаблон в сообщение Claude Code, заполняйте поля,
и получайте точный результат с первого раза! 🚀